# How to run
When in the main directory:
```bash
mkdir build
cd build
cmake ..
cmake --build .
```
then to run the binary:
```bash
src/app
```
`ctrl` + `c` to exit

## To run the test:
```bash
ctest
```